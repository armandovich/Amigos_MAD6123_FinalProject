import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    center: {
        width: '100%',
        alignItems: 'center'
    },
    marginB: {
        marginBottom: 10
    },
    maxWidth: {
        maxWidth: 220
    }
});